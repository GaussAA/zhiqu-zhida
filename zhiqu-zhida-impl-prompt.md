# 知企智答 · 智能客服 Agent 系统 — 工程实现提示词（Handoff）

> 本提示词自包含，可直接粘贴至新会话执行工程实现。无需参考前序设计会话的上下文。

## 一、项目定位
- **产品名**：知企智答
- **定位**：面向「介绍中国互联网公司」场景的智能客服 Agent 系统（对外产品演示 / 求职作品集）。
- **求职方向**：大模型 Agent / 架构师。实现需体现真实工程能力（RAG、编排、评估），而非玩具 demo。
- **现状**：UI 设计已完成（六页明暗双主题 + 作品集），本阶段目标是从设计稿落地为**可运行的 Agent 系统**。

## 二、三大核心模块（与设计稿一一对应）
1. **对话主界面**：多轮对话、快捷问题、知识库检索内联、来源引用展示。
2. **知识库管理后台**：企业条目录入（名称/股票代码/主营业务/行业/状态）、行业与状态筛选、统计概览。
3. **对话日志审查页**：会话列表、命中率/兜底率指标、意图与命中标签、日志导出。

## 三、设计资产（视觉与交互蓝本）
- **Ardot 设计源文件**：fileId `708484225457422`，共 6 页（01/02/03 亮色 + 01/02/03 暗色），每页含主题切换控件。
- **作品集页（含六页截图 + 设计系统说明）**：`portfolio/index.html`（相对项目根 `.workbuddy/portfolio/index.html`）。
- **设计系统精髓（供前端对齐）**：
  - 亮色：画布 `#FAFAFA` / 卡片 `#F4F4F5` / 主文 `#18181B` / 次文 `#71717A` / 边框 黑 8% 发丝线。
  - 暗色：画布 `#09090B` / 卡片 `#18181B` / 主文 `#FAFAFA` / 次文 `#A1A1AA` / 边框 白 7% 发丝线。
  - 强调色：蓝 `#2563EB`(亮)/`#60A5FA`(暗)、绿 `#16A34A`/`#23B980`、琥珀 `#D97706`/`#FBBF24`。
  - 原则：中性灰阶、克制发丝线、低饱和柔底徽章、去除高饱和块面与彩色投影的「AI 味」。
- **注意**：工程会话不必接入 Ardot，以作品集截图/HTML 为视觉参考即可；若需改设计再单独开设计会话。

## 四、技术约束
- **框架**：优先使用 **deepagents 框架**。请先确认其可用性/版本；若当前环境无该框架，先调研等价方案或向用户确认，勿盲造轮子。
- **模型**：优先使用免费模型 `sensenova/sensenova-6.7-flash-lite`（OpenAI 兼容 endpoint `https://token.sensenova.cn/v1/chat/completions`），凭证走环境变量 `SENSENOVA_API_KEY`，**严禁硬编码**。付费模型仅作兜底。
- **运行时**：Python 3.13（uv 虚拟环境，禁止全局安装）/ Node 22（pnpm，禁止 npm）。按子任务选型。
- **安全**：密钥零信任，所有敏感信息走环境变量/`.env`（加入 `.gitignore`）。

## 五、功能需求拆解
### 模块 A：企业知识库（对应设计页 02）
- 数据结构：`企业 { 名称, 股票代码, 主营业务, 行业, 状态(已发布/审核中), 知识文本 }`。
- 能力：CRUD 录入、行业/状态筛选、向量化建库。
- 种子数据：腾讯、阿里、字节、百度 四家，含简介与主营业务知识文本（可联网检索公开资料）。

### 模块 B：对话主界面（对应设计页 01）
- RAG 流程：用户问题 → 意图识别 → 知识库向量检索 → 组织回答 + 标注来源引用。
- 兜底：未命中知识库时给出兜底话术并打「兜底」标记。
- 支持多轮上下文与快捷问题触发。

### 模块 C：对话日志审查（对应设计页 03）
- 记录每轮会话：`{ 会话ID, 用户问题, 识别结果(意图/命中/兜底), 时间 }`。
- 指标：总会话数、命中率、兜底率、平均轮次。
- 支持会话日志导出。

## 六、推荐架构（起点，可按需调整）
- **分层**：数据层(企业知识 + 向量库如 chromadb) / 检索层(embedding + 向量检索) / 编排层(Agent：意图识别 + 工具调用 + RAG) / 服务层(API) / 表现层(Web UI 对齐设计稿)。
- **UI 可选**：React + Tailwind 复刻设计稿明暗双主题；或先以 CLI/API demo 验证核心逻辑，UI 后置。
- **可观测性**：日志审查模块即系统质量评估体现，建议内置结构化日志。

## 七、交付物
- 可本地运行的 Agent 系统（含启动脚本 + README）。
- 企业知识库种子数据。
- 架构图（README 或单独文档）。
- 与三个设计页对应的功能演示（对话 / 知识库管理 / 日志审查）。

## 八、协作与质量约定（用户偏好）
- 里程碑式分步推进，每步可验证、获确认再继续。
- 需求有歧义先澄清，不盲猜意图。
- 显式错误处理、强类型约束、不硬编码密钥。
- 先调研框架/生态再动手（组装优先于手写）。

## 九、建议起步路线（给执行会话）
1. 第 1 步确认 deepagents 框架现状与项目脚手架（先调研，再落地）。
2. 搭最小可跑骨架：载入 4 家企业知识 → 单轮 RAG 问答跑通。
3. 加意图识别 + 兜底标记。
4. 接对话日志审查与指标计算。
5. 视情况补 Web UI 对齐设计稿明暗双主题。
6. 写 README + 架构图，验证端到端 demo。
